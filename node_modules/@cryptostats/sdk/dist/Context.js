"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.Context = void 0;
class Context {
    constructor({ coinGecko, chainData, date, graph, http, ipfs, ethers, etherscan, log, plugins, collection, }) {
        this.coinGecko = coinGecko;
        this.chainData = chainData;
        this.date = date;
        this.graph = graph;
        this.http = http;
        this.ipfs = ipfs;
        this.ethers = ethers;
        this.etherscan = etherscan;
        this.log = log.getLogInterface();
        this.plugins = plugins;
        this.register = (registration) => {
            collection.addAdapter(registration);
        };
        this.registerBundle = (id, metadata) => {
            collection.addBundle(id, metadata);
        };
    }
}
exports.Context = Context;
