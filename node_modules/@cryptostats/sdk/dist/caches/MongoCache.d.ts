interface MongoCacheOptions {
    dbName?: string;
    collectionName?: string;
    client?: any;
}
export declare class MongoCache {
    private client;
    readonly dbName: string;
    readonly collectionName: string;
    constructor(connectionString: string, { dbName, collectionName, client, }?: MongoCacheOptions);
    getValue(name: string, type: string, key: string): Promise<number | null>;
    setValue(name: string, type: string, key: string, value: string | number): Promise<void>;
}
export {};
