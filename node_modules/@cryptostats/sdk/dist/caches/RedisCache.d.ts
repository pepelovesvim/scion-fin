interface RedisCacheOptions {
    redisLibrary?: any;
}
export declare class RedisCache {
    private client;
    private get;
    private set;
    constructor(redisUrl: string, { redisLibrary, }?: RedisCacheOptions);
    getValue(name: string, type: string, key: string): Promise<number | null>;
    setValue(name: string, type: string, key: string, value: string | number): any;
    serialize(name: string, type: string, key: string): string;
}
export {};
