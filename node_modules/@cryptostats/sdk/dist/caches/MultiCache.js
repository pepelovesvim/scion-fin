"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.MultiCache = void 0;
class MultiCache {
    constructor(caches) {
        this.caches = caches;
    }
    async getValue(name, type, key) {
        for (let i = 0; i < this.caches.length; i += 1) {
            let value = await this.caches[i].getValue(name, type, key);
            if (value) {
                await Promise.all(this.caches.slice(0, i).map(cache => cache.setValue(name, type, key, value)));
                return value;
            }
        }
    }
    async setValue(name, type, key, value) {
        await Promise.all(this.caches.map((cache) => cache.setValue(name, type, key, value)));
    }
}
exports.MultiCache = MultiCache;
