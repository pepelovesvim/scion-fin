import { ICache } from '../types';
export declare class MultiCache implements ICache {
    caches: ICache[];
    constructor(caches: ICache[]);
    getValue(name: string, type: string, key: string): Promise<any>;
    setValue(name: string, type: string, key: string, value: string | number): Promise<void>;
}
